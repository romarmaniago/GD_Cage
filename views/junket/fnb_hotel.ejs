<%- include('../partials/header') %>
<%- include('../partials/loader') %>
<%- include('../partials/sidebar') %>
<%- include('../partials/topbar') %>
<%
	const formatDate = (value) => {
		if (!value) return '-';
		const d = new Date(value);
		const pad = (n) => String(n).padStart(2, '0');
		const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
		return `${months[d.getMonth()]} ${d.getDate()}, ${d.getFullYear()} ${pad(d.getHours())}:${pad(d.getMinutes())}`;
	};

	const paymentLabel = (transactionId) => {
		switch (transactionId) {
			case 1:
				return 'Cash';
			case 2:
				return 'Deposit';
			case 3:
				return 'Settle';
			default:
				return '-';
		}
	};
%>

<div class="container-fluid content-inner py-0">
	<div class="row gy-3">
		<div class="col-12">
			<div class="card">
				<div id="user-role" data-permissions="<%= permissions %>"></div>
				<div class="card-header d-flex flex-wrap justify-content-between align-items-center gap-2">
					<h3 class="card-title mb-0">
						<div id="fnb-hotel-filter" class="breadcrumb1 flat">
							<a href="/dashboard"><span class="fa fa-home"></span></a>
							<a href="#"><%= __('fnb_hotel.junket') %></a>
							<a href="#" class="filter-link active" data-filter="all"><%= __('fnb_hotel.all') %></a>
							<span id="fnb-hotel-filter-categories"></span>
						</div>
					</h3>
					<div class="d-flex flex-wrap gap-2 align-items-center">
						<button type="button" id="btn-fnb-hotel-settle" class="btn btn-sm btn-primary">
							<%= __('house_expense.settle') || 'Settle' %>
						</button>
						<% if (typeof permissions !== 'undefined' && permissions !== 2) { %>
						<button type="button" id="btn-fnb-hotel-service-category" class="btn btn-sm btn-info" onclick="openServicesCategoryModal()">
							<i class="fa fa-tags" aria-hidden="true"></i> <%= __('fnb_hotel.service_category') %>
						</button>
						<% } %>
						
						<button type="button" id="btn-fnb-hotel-print" class="btn btn-sm btn-alt-success">
							<i class="fa fa-print" aria-hidden="true"></i> Print
						</button>
						<button type="button" id="btn-fnb-hotel-export" class="btn btn-sm btn-success">
							<i class="fa fa-file-excel" aria-hidden="true"></i> <%= __('fnb_hotel.export') || 'Export' %>
						</button>
					</div>
				</div>
				<div id="fnb-hotel-daterange-mount" class="fnb-hotel-daterange-mount">
					<div class="fnb-hotel-date-filter-wrap mb-0">
						<div class="fnb-hotel-daterange-group d-flex flex-wrap align-items-center gap-2">
							<div class="fnb-hotel-daterange-wrap">
								<input type="text" id="fnb-hotel-daterange" class="form-control form-control-sm text-center fnb-hotel-daterange-input" placeholder="<%= __('fnb_hotel.select_date_range') || 'Select date range' %>" autocomplete="off" />
							</div>
							<%- include('../partials/split_daterange', {
								splitPrefix: 'fnb-hotel',
								splitLocaleNs: 'fnb_hotel',
								splitWrapClass: 'fnb-hotel-daterange-wrap fnb-hotel-split-daterange junket-loss-split-daterange house-expense-split-daterange'
							}) %>
						</div>
					</div>
				</div>
				<div class="card-body fnb-hotel-page">
					<div class="row mt-2">
						<div class="col-12">
							
							<div class="marker-balance-dt-wrap marker-balance-card table-responsive">
								<% if (typeof permissions !== 'undefined' && permissions !== 2) { %>
								<button
									type="button"
									id="btn-fnb-hotel-new-record"
									class="expense-cat-panel-add-btn js-fnb-hotel-add-record d-none"
									data-service-add-trigger
									title="<%= __('fnb_hotel.new_record') %>"
									aria-label="<%= __('fnb_hotel.new_record') %>"
									data-view-only-disable
								>
									<i class="fa fa-plus" aria-hidden="true"></i>
								</button>
								<% } %>
								<table id="fnb-hotel-table" class="table marker-balance-tbl w-100 mb-0">
									<thead>
											<tr>
												<th><%= __('fnb_hotel.program_date') || 'Program Date' %></th>
												<th><%= __('fnb_hotel.date') %></th>
												<th><%= __('fnb_hotel.agent') %></th>
												<th><%= __('fnb_hotel.guest') || 'Guest' %></th>
												<th><%= __('fnb_hotel.type') || 'Type' %></th>
												<th><%= __('fnb_hotel.amount') %></th>
												<th><%= __('fnb_hotel.payment') %></th>
												<th><%= __('fnb_hotel.remarks') %></th>
												<th class="text-center"><%= __('fnb_hotel.action') %></th>
											</tr>
									</thead>
									<tbody>
									</tbody>
								</table>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>

<style>
/* Cream/light-gray table theme */
.card {
    background: linear-gradient(180deg, #f6f3ec 0%, #efe9db 100%) !important;
    border: 1px solid rgba(200, 162, 76, 0.4) !important;
}
.card-header {
    background: linear-gradient(180deg, #f6f3ec 0%, #efe9db 100%) !important;
    border-bottom: 1px solid rgba(200, 162, 76, 0.25) !important;
}
.marker-balance-card {
    background: linear-gradient(180deg, #fafbfc 0%, #ffffff 48px) !important;
    border: 1px solid rgba(15, 23, 42, 0.08) !important;
    border-radius: 12px !important;
    box-shadow: 0 1px 3px rgba(15, 23, 42, 0.04) !important;
    padding: 0 !important;
}

.fnb-hotel-page table.dataTable {
    border-collapse: collapse !important;
    border-spacing: 0 !important;
    table-layout: fixed !important;
}
.fnb-hotel-page table thead tr,
.fnb-hotel-page table tfoot tr {
    background-color: #f5f5f5 !important;
}
.fnb-hotel-page table thead th,
.fnb-hotel-page table tfoot th {
    background: #f5f5f5 !important;
    background-color: #f5f5f5 !important;
    border-left: 0 !important;
    border-right: 0 !important;
    border-top: 0 !important;
    box-shadow: none !important;
}
.fnb-hotel-page table.dataTable thead > tr > th.sorting,
.fnb-hotel-page table.dataTable thead > tr > th.sorting_asc,
.fnb-hotel-page table.dataTable thead > tr > th.sorting_desc,
.fnb-hotel-page table.dataTable thead > tr > th.sorting_disabled {
    background: #f5f5f5 !important;
    background-color: #f5f5f5 !important;
}

/* Base table styles */
#fnb-hotel-table {
    width: 100%;
    table-layout: fixed;
    font-size: 14px;
    border-collapse: collapse;
    border-spacing: 0;
    background-color: #ffffff;
    border: 1px solid #e0e0e0 !important;
}

#fnb-hotel-table thead th:nth-child(1),
#fnb-hotel-table tbody td:nth-child(1) { width: 8%; }   /* Program Date */
#fnb-hotel-table thead th:nth-child(2),
#fnb-hotel-table tbody td:nth-child(2) { width: 11%; }  /* Date */
#fnb-hotel-table thead th:nth-child(3),
#fnb-hotel-table tbody td:nth-child(3) { width: 18%; }  /* Account */
#fnb-hotel-table thead th:nth-child(4),
#fnb-hotel-table tbody td:nth-child(4) { width: 14%; }  /* Guest */
#fnb-hotel-table thead th:nth-child(5),
#fnb-hotel-table tbody td:nth-child(5) { width: 9%; }   /* Type */
#fnb-hotel-table thead th:nth-child(6),
#fnb-hotel-table tbody td:nth-child(6) { width: 8%; }   /* Amount */
#fnb-hotel-table thead th:nth-child(7),
#fnb-hotel-table tbody td:nth-child(7) { width: 7%; }   /* Payment */
#fnb-hotel-table thead th:nth-child(8),
#fnb-hotel-table tbody td:nth-child(8) { width: 17%; }  /* Remarks */
#fnb-hotel-table thead th:nth-child(9),
#fnb-hotel-table tbody td:nth-child(9) { width: 8%; }   /* Action */

#fnb-hotel-table thead th,
#fnb-hotel-table tbody td {
    vertical-align: middle;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

/* Header styles */
#fnb-hotel-table thead th {
    background: #f5f5f5 !important;
    background-color: #f5f5f5 !important;
    color: #555 !important;
    font-weight: 600;
    padding: 0.4rem 0.55rem;
    text-align: left;
    border: none;
    border-bottom: 1px solid #e0e0e0 !important;
}

#fnb-hotel-table thead th:nth-child(6),
#fnb-hotel-table tbody td:nth-child(6) {
    text-align: right !important;
    padding-right: 0.75rem;
}

#fnb-hotel-table thead th:nth-child(7),
#fnb-hotel-table tbody td:nth-child(7),
#fnb-hotel-table thead th:last-child,
#fnb-hotel-table tbody td:last-child {
    text-align: center !important;
}

#fnb-hotel-table tbody td:last-child .btn-group {
    display: inline-flex;
    vertical-align: middle;
}

/* Row styles */
#fnb-hotel-table tbody tr {
    border-bottom: 1px solid #eee;
    transition: background-color 0.2s ease;
}

#fnb-hotel-table tbody tr:nth-child(even) td {
    background-color: #f4f1ea !important;
}
#fnb-hotel-table tbody tr:nth-child(odd) td {
    background-color: #fff !important;
}

#fnb-hotel-table tbody tr:hover,
#fnb-hotel-table tbody tr:hover td {
    background-color: #f6f3ec !important;
}

/* Cell styles */
#fnb-hotel-table td {
    padding: 0.35rem 0.55rem;
    color: #666;
    text-align: left;
}

.fnb-hotel-date-filter-wrap {
    margin: 0;
}

.fnb-hotel-daterange-group {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px;
}

.fnb-hotel-daterange-wrap {
    width: auto;
    min-width: 0;
    max-width: none;
    flex: 0 0 auto;
    display: inline-block;
}

.fnb-hotel-daterange-wrap .flatpickr-wrapper {
    display: inline-block;
    width: auto;
    min-width: 0;
}

.fnb-hotel-daterange-wrap input.form-control,
.fnb-hotel-daterange-wrap .flatpickr-input.form-control {
    display: inline-block !important;
    position: static !important;
    left: auto !important;
    top: auto !important;
    width: auto !important;
    min-width: 10rem;
    max-width: none !important;
    margin: 0;
    overflow: visible;
    text-overflow: clip;
    white-space: nowrap;
    box-sizing: border-box;
    text-align: center;
    cursor: pointer;
    background: #fff;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    font-size: 13px;
    height: 34px;
}

.fnb-hotel-split-daterange,
.junket-loss-split-daterange,
.house-expense-split-daterange {
    display: inline-flex;
    align-items: center;
    flex-shrink: 0;
    min-width: 0;
    gap: 0.5rem;
    flex-wrap: nowrap;
}

.house-expense-daterange-separator {
    color: #667085;
    font-size: 0.8125rem;
    font-weight: 500;
    line-height: 1;
    user-select: none;
    white-space: nowrap;
}

.fnb-hotel-split-daterange .flatpickr-wrapper,
.junket-loss-split-daterange .flatpickr-wrapper,
.house-expense-split-daterange .flatpickr-wrapper {
    display: inline-block;
    width: 7.5rem;
    min-width: 7.5rem;
}

.fnb-hotel-split-daterange input.split-date-input,
.fnb-hotel-split-daterange .flatpickr-input.form-control,
.junket-loss-split-daterange input.split-date-input,
.junket-loss-split-daterange .flatpickr-input.form-control,
.house-expense-split-daterange input.split-date-input,
.house-expense-split-daterange .flatpickr-input.form-control {
    display: inline-block;
    position: static;
    left: auto;
    top: auto;
    width: 7.5rem !important;
    min-width: 7.5rem !important;
    max-width: 7.5rem !important;
    padding-left: 0.9rem;
    padding-right: 0.9rem;
    margin: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    box-sizing: border-box;
    cursor: pointer;
    text-align: center;
    field-sizing: fixed;
    background: #fff;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    font-size: 13px;
    height: 34px;
}

/* No-data row: hidden by default, shown only with .show-empty */
#fnb-hotel-table tbody tr.no-data-row {
    display: none !important;
}
#fnb-hotel-table tbody tr.no-data-row.show-empty {
    display: table-row !important;
}

/* Footer total row */
#fnb-hotel-table tfoot th {
    padding: 0.4rem 0.55rem;
    font-weight: 600;
    background: #f5f5f5 !important;
    color: #555 !important;
    border-top: 1px solid #e0e0e0 !important;
    border-bottom: 1px solid #e0e0e0 !important;
}

.select2-container .select2-selection--single {
							height: calc(1.5em + 0.75rem + 6px);
						}

						.select2-container .select2-search--inline .select2-search__field {
							margin-top: 0;
							margin: 0.25rem 0.25rem 0.25rem 0;
							height: 1.375rem;
							line-height: 1.375rem;
						}

						.select2-container--default .select2-selection--single {
							border-color: #eee;

						}

						.select2-container--default .select2-selection--single .select2-selection__rendered {
							display: flex;
							align-items: center;
							padding-left: 0.75rem;
							height: calc(1.5em + 0.75rem + 6px);
							line-height: 1.5;
						}

						.select2-container--default .select2-selection--single .select2-selection__arrow {
							height: calc(1.5em + 0.75rem + 6px);
						}

						.select2-container--default .select2-selection--single .select2-selection__placeholder {
							color: #8A92A6;
						}

						.select2-container--default .select2-selection--multiple .select2-selection__rendered {
							padding-right: 0.75rem;
							padding-left: 0.75rem;
						}

#btn-fnb-hotel-new-record.expense-cat-panel-add-btn {
	display: inline-flex;
	align-items: center;
	justify-content: center;
	width: 2.25rem;
	height: 2.25rem;
	padding: 0;
	border-color: transparent;
	border-radius: 50%;
	background: linear-gradient(135deg, #52525b 0%, #71717a 100%);
	color: #fff;
	line-height: 1;
	flex-shrink: 0;
	position: relative;
	z-index: 2;
	pointer-events: auto;
	cursor: pointer;
	transition: background 0.12s ease, color 0.12s ease, border-color 0.12s ease;
	margin-right: 8px;
	vertical-align: middle;
}

#btn-fnb-hotel-new-record.expense-cat-panel-add-btn i.fa {
	font-size: 1rem;
}

#btn-fnb-hotel-new-record.expense-cat-panel-add-btn:hover:not(:disabled) {
	filter: brightness(0.92);
}

#btn-fnb-hotel-new-record.expense-cat-panel-add-btn:disabled {
	opacity: 0.45;
	cursor: not-allowed;
}

#fnb-hotel-table_wrapper .fnb-hotel-controls-highlight {
	display: flex;
	align-items: center;
	justify-content: space-between;
	flex-wrap: wrap;
	gap: 12px;
	padding: 8px 12px;
	margin: 0;
	background: #fff;
	border: 1px solid #e8ecf1;
	border-bottom: 0;
	border-radius: 10px 10px 0 0;
}

#fnb-hotel-table_wrapper .fnb-hotel-controls-highlight .dataTables_length {
	margin-top: 4px;
}

#fnb-hotel-table_wrapper .fnb-hotel-controls-highlight .dataTables_filter {
	margin: 0 0 0 auto;
}

#fnb-hotel-daterange-mount {
	display: none;
}

#fnb-hotel-table_wrapper .fnb-hotel-daterange-mount.is-placed {
	display: inline-flex;
	align-items: center;
	margin-left: 8px;
}

#fnb-hotel-table_wrapper .dataTables_scroll,
#fnb-hotel-table_wrapper .dataTables_scrollHead,
#fnb-hotel-table_wrapper .dataTables_scrollHeadInner,
#fnb-hotel-table_wrapper table.dataTable {
	margin-top: 0 !important;
	margin-bottom: 0 !important;
	border-top: 0 !important;
}

#fnb-hotel-table_wrapper > .row.fnb-hotel-dt-top-row-empty {
	display: none !important;
	margin: 0 !important;
	padding: 0 !important;
	height: 0 !important;
}

#fnb-hotel-table_filter .fnb-hotel-filter-highlight {
	display: inline-flex;
	align-items: center;
	gap: 8px;
	padding: 2px 6px;
	background: rgba(255, 255, 255, 0.35);
	border: none;
	border-radius: 10px;
}

#fnb-hotel-table_filter .fnb-hotel-filter-highlight label {
	margin: 0;
	display: inline-flex;
	align-items: center;
	gap: 8px;
}

#fnb-hotel-table_filter .fnb-hotel-filter-highlight label input {
	margin-left: 0;
	border: none;
	outline: none;
	box-shadow: none;
	background: #fff;
}

</style>
<%- include ('../partials/permission_view_only_script') %>
<%- include('../modals/junket/new_service_record') %>
<%- include('../modals/junket/edit_service_record') %>
<%- include('../partials/fnb_hotel_receipt_modal') %>
<%- include('../partials/footer') %>
<script>
	// Pass translations to JavaScript
	window.fnbHotelTranslations = {
		missing_fields: "<%= __('fnb_hotel.missing_fields') %>",
		select_who_is_paying: "<%= __('fnb_hotel.select_who_is_paying') %>",
		select_service_type: "<%= __('fnb_hotel.select_service_type') %>",
		select_account: "<%= __('fnb_hotel.select_account') %>",
		select_guest_optional: "<%= __('fnb_hotel.select_guest_optional') %>",
		select_service: "<%= __('fnb_hotel.select_service') %>",
		select_transaction_type: "<%= __('fnb_hotel.select_transaction_type') %>",
		select_program_date: "<%= __('fnb_hotel.select_program_date') || 'Select a program date.' %>",
		program_date: "<%= __('fnb_hotel.program_date') || 'Program Date' %>",
		saving: "<%= __('fnb_hotel.saving') %>",
		updating: "<%= __('fnb_hotel.updating') %>",
		service_recorded: "<%= __('fnb_hotel.service_recorded') %>",
		service_updated: "<%= __('fnb_hotel.service_updated') %>",
		invalid_amount: "<%= __('fnb_hotel.invalid_amount') %>",
		amount_must_be_greater: "<%= __('fnb_hotel.amount_must_be_greater') %>",
		insufficient_balance: "<%= __('fnb_hotel.insufficient_balance') %>",
		amount_exceeds_balance: "<%= __('fnb_hotel.amount_exceeds_balance') %>",
		service_id_missing: "<%= __('fnb_hotel.service_id_missing') %>",
		failed_to_update: "<%= __('fnb_hotel.failed_to_update') %>",
		update: "<%= __('fnb_hotel.update') %>",
		error: "<%= __('fnb_hotel.error') %>",
		failed_to_save: "<%= __('fnb_hotel.failed_to_save') %>",
		save: "<%= __('fnb_hotel.save') %>",
		showing_entries: "<%= __('fnb_hotel.showing_entries') %>",
		previous: "<%= __('fnb_hotel.previous') %>",
		next: "<%= __('fnb_hotel.next') %>",
		search: "<%= __('fnb_hotel.search') %>",
		no_data_available: "<%= __('fnb_hotel.no_data_available') %>",
		delete_confirmation: "<%= __('fnb_hotel.delete_confirmation') %>",
		yes_delete: "<%= __('fnb_hotel.yes_delete') %>",
		cancelled: "<%= __('fnb_hotel.cancelled') %>",
		delete_cancelled: "<%= __('fnb_hotel.delete_cancelled') %>",
		deleted: "<%= __('fnb_hotel.deleted') %>",
		service_deleted: "<%= __('fnb_hotel.service_deleted') %>",
		delete_failed: "<%= __('fnb_hotel.delete_failed') %>",
		failed_to_delete: "<%= __('fnb_hotel.failed_to_delete') %>",
		error: "<%= __('fnb_hotel.error') %>",
		export_label: "<%= __('fnb_hotel.export') || 'Export' %>"
	};
</script>
<script src="/assets/js/functions/service_category_select.js"></script>
<script src="/assets/js/functions/new_service_record.js"></script>
<script src="/assets/js/functions/edit_service_record.js"></script>
<script src="/assets/js/functions/fnb_hotel_receipt.js"></script>
<script src="/assets/js/functions/fnb_hotel.js"></script>
<script>
	(function () {
		var dateRangeMount = document.getElementById('fnb-hotel-daterange-mount');

		function moveNewRecordButtonBesideSearch() {
			var wrapper = document.getElementById('fnb-hotel-table_wrapper');
			var lengthWrap = document.getElementById('fnb-hotel-table_length');
			var filterWrap = document.getElementById('fnb-hotel-table_filter');
			var searchLabel = filterWrap ? filterWrap.querySelector('label') : null;
			var addBtn = document.getElementById('btn-fnb-hotel-new-record');
			var controlsHighlight;
			var highlightBox;
			if (!wrapper || !lengthWrap || !filterWrap || !searchLabel) return;

			controlsHighlight = wrapper.querySelector('.fnb-hotel-controls-highlight');
			if (!controlsHighlight) {
				controlsHighlight = document.createElement('div');
				controlsHighlight.className = 'fnb-hotel-controls-highlight';
				wrapper.insertBefore(controlsHighlight, wrapper.firstChild);
			}
			if (lengthWrap.parentElement !== controlsHighlight || controlsHighlight.firstElementChild !== lengthWrap) {
				controlsHighlight.insertBefore(lengthWrap, controlsHighlight.firstChild);
			}

			if (dateRangeMount) {
				if (dateRangeMount.parentElement !== controlsHighlight || lengthWrap.nextElementSibling !== dateRangeMount) {
					if (lengthWrap.nextSibling) {
						controlsHighlight.insertBefore(dateRangeMount, lengthWrap.nextSibling);
					} else {
						controlsHighlight.appendChild(dateRangeMount);
					}
				}
				dateRangeMount.classList.add('is-placed');
			}

			if (filterWrap.parentElement !== controlsHighlight) {
				controlsHighlight.appendChild(filterWrap);
			}

			highlightBox = filterWrap.querySelector('.fnb-hotel-filter-highlight');
			if (!highlightBox) {
				highlightBox = document.createElement('div');
				highlightBox.className = 'fnb-hotel-filter-highlight';
				filterWrap.appendChild(highlightBox);
			}
			if (addBtn && (addBtn.parentElement !== highlightBox || highlightBox.firstElementChild !== addBtn)) {
				highlightBox.insertBefore(addBtn, highlightBox.firstChild);
				addBtn.classList.remove('d-none');
			}
			if (searchLabel.parentElement !== highlightBox) {
				highlightBox.appendChild(searchLabel);
			}

			Array.prototype.forEach.call(wrapper.children, function (row) {
				if (!row.classList || !row.classList.contains('row')) return;
				if (row.classList.contains('dt-row') || row.querySelector('table')) return;
				if (!row.querySelector('.dataTables_length, .dataTables_filter, .dataTables_info, .dataTables_paginate')) {
					row.classList.add('fnb-hotel-dt-top-row-empty');
				}
			});
		}

		if (window.jQuery) {
			$(document).on('init.dt draw.dt', '#fnb-hotel-table', moveNewRecordButtonBesideSearch);
			$(moveNewRecordButtonBesideSearch);
		} else {
			document.addEventListener('DOMContentLoaded', function () {
				setTimeout(moveNewRecordButtonBesideSearch, 0);
			});
		}
	})();
</script>
<% if (typeof permissions !== 'undefined' && permissions !== 2) { %>
<script>
	window.servicesCategoryTranslations = {
		search: "<%= __('services_category.search') %>",
		showing_entries: "<%= __('services_category.showing_entries') %>",
		previous: "<%= __('services_category.previous') %>",
		next: "<%= __('services_category.next') %>",
		active: "<%= __('services_category.active') %>",
		inactive: "<%= __('services_category.inactive') %>"
	};
</script>
<script src="/assets/js/functions/services_category.js"></script>
<%- include('../modals/junket/manage_services_category') %>
<%- include('../modals/popups/new_services_category') %>
<%- include('../modals/popups/edit_services_category') %>
<% } %>

