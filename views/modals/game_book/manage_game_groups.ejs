<div class="modal fade" id="modal-manage-game-groups" role="dialog" aria-labelledby="modal-manage-game-groups-title"
	aria-hidden="true" data-keyboard="false" data-bs-backdrop="static">
	<div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
		<div class="modal-content">
			<div class="modal-header d-flex justify-content-between align-items-center" style="background: linear-gradient(180deg, #f6f3ec 0%, #efe9db 100%); border-bottom: 1px solid rgba(200, 162, 76, 0.25);">
				<h5 class="block-title mb-0" style="color: #555; font-weight: 600;" id="modal-manage-game-groups-title">Manage Groups</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<div class="modal-body">
				<div class="d-flex justify-content-end">
					<button
						type="button"
						class="game-group-panel-add-btn"
						id="btn-add-game-group"
						title="New Group"
						aria-label="New Group"
						onclick="addGameGroup()"
					>
						<i class="fa fa-plus" aria-hidden="true"></i>
					</button>
				</div>
				<div class="table-responsive">
					<table id="game-group-tbl" class="table nowrap small-text" style="width:100%">
						<thead>
							<tr>
								<th class="d-none d-sm-table-cell">Group Name</th>
								<th class="d-none d-sm-table-cell" style="width: 100px;">Action</th>
							</tr>
						</thead>
						<tbody></tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
</div>

<style>
	#modal-manage-game-groups .modal-body { overflow-x: auto; }
	#game-group-tbl { width: 100%; font-size: 15px; border-collapse: collapse; border: 1px solid #e0e0e0; }
	#game-group-tbl thead tr { background-color: #f5f5f5; }
	#game-group-tbl thead th {
		background: #f5f5f5 !important;
		color: #555;
		font-weight: 600;
		padding: 10px 12px;
		text-align: left;
		border: none;
		border-bottom: 1px solid #e0e0e0;
		box-shadow: none !important;
	}
	#game-group-tbl tbody tr { border-bottom: 1px solid #eee; }
	#game-group-tbl tbody tr:nth-child(even) td { background-color: #f4f1ea; }
	#game-group-tbl tbody tr:nth-child(odd) td { background-color: #fff; }
	#game-group-tbl td { padding: 10px 12px; color: #666; }
	#game-group-tbl tbody tr:hover, #game-group-tbl tbody tr:hover td { background-color: #f6f3ec !important; }
	#game-group-tbl th, #game-group-tbl td { white-space: nowrap; }
	#game-group-tbl .game-group-default-label { color: #8a92a6; font-style: italic; font-size: 0.85em; }

	#btn-add-game-group.game-group-panel-add-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 26px;
		height: 26px;
		padding: 0;
		margin-bottom: 12px;
		border: none;
		border-radius: 50%;
		background: linear-gradient(135deg, #52525b 0%, #71717a 100%);
		color: #fff;
		line-height: 1;
		cursor: pointer;
		transition: filter 0.12s ease;
	}
	#btn-add-game-group.game-group-panel-add-btn i.fa {
		font-size: 13px;
		line-height: 1;
		pointer-events: none;
	}
	#btn-add-game-group.game-group-panel-add-btn:hover {
		filter: brightness(0.92);
	}

	#modal-new-game-group,
	#modal-edit-game-group { z-index: 1060 !important; }
</style>
