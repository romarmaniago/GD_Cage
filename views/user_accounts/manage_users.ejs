<%- include('../partials/header') %>
<%- include('../partials/loader') %>
	<%- include('../partials/sidebar') %>

		<%- include('../partials/topbar') %>

			<div class="container-fluid content-inner py-0">
				<div class="row">
					<div class="col-sm-12">
						<div class="card">
							<div id="user-role" data-permissions="<%= permissions %>"></div>
							<div class="card-header d-flex justify-content-between align-items-center">
								
								<h3 class="card-title"><div class="breadcrumb1 flat">
									<a href="/dashboard" ><span class="fa fa-home"></span></a>
									<a href="/manage_users"class="active"><%= __('manage_users.manage_users') %></a>
								</div></h3>
								
							</div>


							<div class="card-body settings-page">
								<button
									type="button"
									class="expense-cat-panel-add-btn d-none"
									id="btn-add-user"
									title="<%= __('manage_users.new_user') %>"
									aria-label="<%= __('manage_users.new_user') %>"
									onclick="add_user_modal()"
									data-view-only-disable
								>
									<i class="fa fa-plus" aria-hidden="true"></i>
								</button>
								<div class="marker-balance-dt-wrap marker-balance-card table-responsive">
								<table id="usersTable" class="table marker-balance-tbl w-100 mb-0">
									<thead>
										<tr>
											<th class="d-none d-sm-table-cell"><%= __('manage_users.last_name') %></th>
											<th class="d-none d-sm-table-cell"><%= __('manage_users.first_name') %></th>
											<th class="d-none d-sm-table-cell"><%= __('manage_users.username') %></th>
											<th class="d-none d-sm-table-cell"><%= __('manage_users.permission') %></th>
											<th class="d-none d-sm-table-cell"><%= __('manage_users.status') %></th>
											<th class="d-none d-sm-table-cell text-center"><%= __('manage_users.action') %></th>
										</tr>
									</thead>
									<tbody>

									</tbody>
								</table>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>


			<%- include ('../modals/user_accounts/new_user_account'); %>
				<%- include ('../modals/user_accounts/edit_user_account'); %>
				<%- include ('../modals/user_accounts/change_user_password'); %>
				<%- include ('../partials/permission_view_only_script') %>
					<%- include ('../partials/footer'); -%>

					<script>
						// Pass translations to JavaScript
						window.manageUsersTranslations = {
							search: "<%= __('manage_users.search') %>",
							showing_entries: "<%= __('manage_users.showing_entries') %>",
							previous: "<%= __('manage_users.previous') %>",
							next: "<%= __('manage_users.next') %>",
							active: "<%= __('manage_users.active') %>",
							inactive: "<%= __('manage_users.inactive') %>"
						};

						(function () {
							function applyManageUsersControlsLayout() {
								var wrapper = document.getElementById('usersTable_wrapper');
								var lengthWrap = document.getElementById('usersTable_length');
								var filterWrap = document.getElementById('usersTable_filter');
								var searchLabel = filterWrap ? filterWrap.querySelector('label') : null;
								var searchInput = searchLabel ? searchLabel.querySelector('input') : null;
								var addBtn = document.getElementById('btn-add-user');
								var controlsHighlight;
								var filterHighlight;
								var searchPlaceholder = (window.manageUsersTranslations && window.manageUsersTranslations.search)
									? String(window.manageUsersTranslations.search).replace(/\s*:?\s*$/, '') + '...'
									: 'Search...';

								if (!wrapper || !lengthWrap || !filterWrap || !searchLabel) return;

								controlsHighlight = wrapper.querySelector('.manage-users-controls-highlight');
								if (!controlsHighlight) {
									controlsHighlight = document.createElement('div');
									controlsHighlight.className = 'manage-users-controls-highlight';
									wrapper.insertBefore(controlsHighlight, wrapper.firstChild);
								}
								if (lengthWrap.parentElement !== controlsHighlight) {
									controlsHighlight.appendChild(lengthWrap);
								}
								if (filterWrap.parentElement !== controlsHighlight) {
									controlsHighlight.appendChild(filterWrap);
								}

								filterHighlight = filterWrap.querySelector('.manage-users-filter-highlight');
								if (!filterHighlight) {
									filterHighlight = document.createElement('div');
									filterHighlight.className = 'manage-users-filter-highlight';
									filterWrap.appendChild(filterHighlight);
								}
								if (addBtn && addBtn.parentElement !== filterHighlight) {
									filterHighlight.appendChild(addBtn);
								}
								if (searchLabel.parentElement !== filterHighlight) {
									filterHighlight.appendChild(searchLabel);
								}
								if (searchInput) {
									searchInput.setAttribute('placeholder', searchPlaceholder);
									Array.prototype.slice.call(searchLabel.childNodes).forEach(function (node) {
										if (node.nodeType === 3) searchLabel.removeChild(node);
									});
								}
								if (addBtn) addBtn.classList.remove('d-none');
							}

							if (window.jQuery) {
								$(document).on('init.dt draw.dt', '#usersTable', applyManageUsersControlsLayout);
								$(applyManageUsersControlsLayout);
							} else {
								document.addEventListener('DOMContentLoaded', function () {
									setTimeout(applyManageUsersControlsLayout, 0);
								});
							}
						})();
					</script>

						<script src="/assets/js/functions/manage_user.js?v=5"></script>
						

						<script>
							$('.js-select2').select2({
								placeholder: 'Select an option',
								dropdownParent: '#modal-new_user'
							});
						</script>

						<style>
							.card-body {
  overflow-x: auto;
}

/* Cream/light-gray theme */
.card {
	background: linear-gradient(180deg, #f6f3ec 0%, #efe9db 100%) !important;
	border: 1px solid rgba(200, 162, 76, 0.4) !important;
}
.card-header {
	background: linear-gradient(180deg, #f6f3ec 0%, #efe9db 100%) !important;
	border-bottom: 1px solid rgba(200, 162, 76, 0.25) !important;
}
.marker-balance-card {
	background: linear-gradient(180deg, #fafbfc 0%, #ffffff 48px) !important;
	border: 1px solid rgba(15, 23, 42, 0.08) !important;
	border-radius: 12px !important;
	box-shadow: 0 1px 3px rgba(15, 23, 42, 0.04) !important;
	padding: 0 !important;
}

.settings-page table.dataTable {
	border-collapse: collapse !important;
	border-spacing: 0 !important;
}
.settings-page table thead tr,
.settings-page table tfoot tr {
	background-color: #f5f5f5 !important;
}
.settings-page table thead th,
.settings-page table tfoot th {
	background: #f5f5f5 !important;
	background-color: #f5f5f5 !important;
	box-shadow: none !important;
}
.settings-page table.dataTable thead > tr > th.sorting,
.settings-page table.dataTable thead > tr > th.sorting_asc,
.settings-page table.dataTable thead > tr > th.sorting_desc,
.settings-page table.dataTable thead > tr > th.sorting_disabled {
	background: #f5f5f5 !important;
	background-color: #f5f5f5 !important;
}

#usersTable {
    width: 100%;
    font-size: 13px;
    border-collapse: collapse;
    border: 1px solid #e0e0e0 !important;
}

/* Circular + beside search — same as User Roles */
#btn-add-user.expense-cat-panel-add-btn {
	display: inline-flex;
	align-items: center;
	justify-content: center;
	width: 2.25rem;
	height: 2.25rem;
	padding: 0;
	border-color: transparent;
	border-radius: 50%;
	background: linear-gradient(135deg, #52525b 0%, #71717a 100%);
	color: #fff;
	box-shadow: none;
	line-height: 1;
	flex-shrink: 0;
	position: relative;
	z-index: 2;
	cursor: pointer;
	transition: background 0.12s ease, color 0.12s ease;
}
#btn-add-user.expense-cat-panel-add-btn i.fa {
	font-size: 1rem;
}
#btn-add-user.expense-cat-panel-add-btn:hover:not(:disabled) {
	filter: brightness(0.92);
}
#btn-add-user.expense-cat-panel-add-btn:disabled {
	opacity: 0.45;
	cursor: not-allowed;
}

#usersTable_wrapper .manage-users-controls-highlight {
	display: flex;
	align-items: center;
	justify-content: space-between;
	gap: 12px;
	padding: 8px 12px;
	margin-bottom: -1px;
	background: #fff;
	border: 1px solid #e8ecf1;
	border-bottom: 0;
	border-radius: 10px 10px 0 0;
}
#usersTable_wrapper .manage-users-controls-highlight .dataTables_length {
	margin-top: 4px;
}
#usersTable_wrapper .manage-users-controls-highlight .dataTables_filter {
	margin: 0 0 0 auto;
}
#usersTable_wrapper #usersTable_filter .manage-users-filter-highlight {
	display: inline-flex;
	align-items: center;
	gap: 8px;
	padding: 2px 6px;
	background: transparent;
	border: none;
	border-radius: 10px;
}
#usersTable_wrapper #usersTable_filter .manage-users-filter-highlight label {
	margin: 0;
	display: inline-flex;
	align-items: center;
	gap: 0;
	font-size: 0;
	color: transparent;
}
#usersTable_wrapper #usersTable_filter .manage-users-filter-highlight label input {
	margin-left: 0;
	border: none;
	outline: none;
	box-shadow: none;
	background: #fff;
	border-radius: 4px;
	padding: 6px 10px;
	font-size: 13px;
	color: #333;
	min-width: 160px;
}
#usersTable_wrapper #usersTable_filter .manage-users-filter-highlight label input::placeholder {
	color: #8a92a6;
	opacity: 1;
}
#usersTable_wrapper #usersTable {
	margin-top: 0 !important;
}
#usersTable_wrapper .dataTables_length,
#usersTable_wrapper .dataTables_filter {
	padding-left: 0;
	padding-right: 0;
}

/* Header styles */
#usersTable thead th {
    background: #f5f5f5 !important;
    color: #555 !important;
    font-weight: 600;
    padding: 12px 15px;
    text-align: left;
    border: none;
    border-bottom: 1px solid #e0e0e0 !important;
}

/* Row styles */
#usersTable tbody tr {
    border-bottom: 1px solid #eee;
}

#usersTable tbody tr:nth-child(even) td {
	background-color: #f4f1ea !important;
}
#usersTable tbody tr:nth-child(odd) td {
	background-color: #fff !important;
}

/* Cell styles */
#usersTable td {
    padding: 12px 15px;
    color: #666;
}

#usersTable tbody tr:hover,
#usersTable tbody tr:hover td {
  background-color: #f6f3ec !important;
}

/* Balance column */
#usersTable td:last-child {
    text-align: center;
    color: #333;
}

/* Footer total row */
#usersTable tfoot th {
    padding: 10px;
    font-weight: 600;
    background: #f5f5f5 !important;
    color: #555 !important;
    border-top: 1px solid #e0e0e0 !important;
    border-bottom: 1px solid #e0e0e0 !important;
}
@media (max-width: 768px) {
    #usersTable {
        font-size: 10px; /* Mas maliit sa mobile */
    }

    #usersTable th,
    #usersTable td {
        padding: 5px;
    }
}
							/* Itago ang text na "Show" at "entries" sa DataTables */
							.dataTables_length label {
								font-size: 0; /* Itatago ang text */
							}
							
							/* Panatilihin ang dropdown na nakikita */
							.dataTables_length select {
								font-size: 12px; /* Ibalik ang font size ng dropdown */
							}
							
							
							
								/* Adjust spacing and alignment for date range picker */
							.dataTables_wrapper .row {
								margin-bottom: 15px;
								font-size: 12px; /* Pareho sa commission-tbl */
							}
							
							/* Flexbox layout for the date range picker */
							.input-daterange {
								display: flex;
								justify-content: space-between;
								position: relative;
								left: 7.5rem;
								top: 3.5rem;
								max-width: 300px;
								font-size: 12px; /* Pareho sa commission-tbl */
							}
							
							/* Styling for form controls */
							.input-daterange .form-control {
								flex: 1;
								margin-right: 5px;
								max-width: 120px;
								font-size: 12px; /* Pareho sa commission-tbl */
								padding: 8px; /* Pareho sa cell padding ng table */
							}
							
							/* Remove margin from the last input */
							.input-daterange .form-control:last-child {
								margin-right: 0;
							}
							
							/* Responsive tweaks */
							@media (max-width: 768px) {
								.dataTables_wrapper .row,
								.input-daterange,
								.input-daterange .form-control {
									font-size: 10px; /* Mas maliit sa mobile */
								}
							
								.input-daterange .form-control {
									padding: 5px; /* Mas maliit na padding */
								}
							}
						</style>